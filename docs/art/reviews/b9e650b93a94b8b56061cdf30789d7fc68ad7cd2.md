# Kestrel low sweep and upward smash — Windows review, 2026-09-11

Reviewed candidate: `b9e650b93a94b8b56061cdf30789d7fc68ad7cd2`, isolated on the earlier `882cac54644a131a3716aa379007cb8ffa58e3e4` implementation. Its only changed source file is `src/model/anim_directed.rs`. This is an incremental visual improvement accepted for integration; it does not complete Kestrel or approve Alpha 0.7.

## Source and corrected regression

Claude source archive: `OVERFRAME_visual_fix_source.zip`, SHA-256 `ed32ef3678d5a3c828854e554cb67bfbf476bf75358478825b1a265120e198b3`. Delivered source SHA-256: `dc262376357efff95ebe965ae00b01cd86e36bd79de71b56f08d925eada83254`. The archive and all nine manifest-listed files were verified before transfer. Only the source file is integrated; the archive README, logs and transfer files are preserved separately.

The earlier `f51ea395` delivery was rejected: its new LowSweep chamber buried the attacking foot during startup and the wind-to-contact blend, reaching -1.557433 units at tick 4/state frame 5 in both facings. It also failed formatting. The corrected chamber raises the folded heel without changing the active solve target or globally lifting the body. The corrected source also includes the earlier intended low diagonal, separate counterbalancing arms and deeper supporting knee; upward smash compresses before extending into contact.

The new regression scans both feet throughout the state-frame cycles of both smashes and checks active contact. Claude's included failure log demonstrates that restoring the old chamber makes it fail. This test and its optional diagnostic evaluate the real pose builder on constructed fighter states; they are not themselves input-driven replay or hardware gameplay tests. The separate Windows runtime captures below establish the actual fixture results. The 158 diagnostic pose rows reproduce identically in Windows.

## Windows checks and actual evidence

- Formatter and warnings-denied Clippy, all targets/all features: passed.
- Rust all-feature suite: 163 passed, 0 failed, 4 ignored report-only diagnostics.
- Without default features, with `netcode gltf`: 156 passed, 0 failed, 4 ignored.
- Python evidence tools: 21 passed. The additional foot diagnostic was explicitly run and passed.
- Release build: passed. Executable SHA-256 `5a75530fcfae9d3d861bbd089c7dc4248a9594ca89c59c7ff1259a86d00883d7`.
- Production capture on Windows/NVIDIA OpenGL: 60 files, 0 skipped. Independent checks: 234 active contacts passed, 1,662 support measurements valid, 264 rendered cells match replay, and 2,648 grounded foot measurements contain no penetration beyond 0.01 units.
- All 2,743 simulation CSV rows and character/stage/frame-data tables match `019c2b1`, excluding source labels. Only `dsmash-cstick.json` and `usmash-cstick.json` differ among the contact cases.

Actual Windows primary and wide images were inspected for both attacks and both facings. The low sweep keeps the folded leg above the floor, extends on a lower diagonal from a bent supporting knee, then folds and settles once. Upward smash now visibly rises from its load into the active strike. Its first-active support remains +0.6475/+1.0891 units, with the sheet reporting the minimum +0.65; this is a small positive clearance, not zero or penetration.

Separate motion captures completed with zero skips: 82 frames for dsmash and 84 for usmash, both facings, including the pre/post states. All 166 frames were inspected in labelled tracking contact sheets derived from the original fixed-camera images. The late hold and recovery preserve a single strike and return to guard. This establishes frame-by-frame pose review, not a claim of interactive real-time gameplay or physical-controller testing.

Local evidence is preserved under the review workspace's `work/game-evidence/windows-b9e650b`, its `-dsmash-motion`/`-usmash-motion` directories and corresponding `-all-frames` review sheets. These are local review artifacts, not a published CI evidence commit.

Claude's supplementary Linux evidence archive has SHA-256 `90b10cae8da449e9df835354277b074fa22c983077d1ab010e178f2a3dd4a360`; all 183 manifest-listed files were verified. It now explicitly labels itself `WIP-uncommitted-on-c6966f3d-anim_directed-sha256-dc262376357efff95ebe965ae00b01cd86e36bd79de71b56f08d925eada83254`. The earlier base-commit labels were inaccurate. This review's acceptance relies on the independently built, clean candidate and its Windows captures.

## Next implementation

Proceed to the [gameplay corrections established by the Windows baseline](../procedural/anim/kestrel-gameplay-fixes.md): projectile shielding, supported shield-entry emission, preservation of existing owner hitlag, deliberate removal of Kestrel's zero-damage melee placeholder, and actual rearward launch regression/fixes. Follow that with the compact original SpecialN discharge. Keep the baseline limitations and before/after tests explicit. Kestrel's full art gate, gameplay-camera work and later Alpha phases remain pending; no version bump or release tag is authorized by this review.

## 2026-09-11 — Integrated source and published evidence

The reviewed source was integrated on game main as `15b83866761adbb4f53f46e50d54bd62737124d9`. CI [34590337321](https://github.com/Gisleno-bit/OverFrame/actions/runs/34590337321) and visual evidence [34590337365](https://github.com/Gisleno-bit/OverFrame/actions/runs/34590337365) both completed successfully. The immutable evidence commit is `27546613571aa3d0a9a0ea55c90ded532efa3405`, under `builds/15b83866761adbb4f53f46e50d54bd62737124d9/attempt-34590337365-1/`. All 89 extracted evidence files matched their manifest sizes and SHA-256 hashes. The actual CI dsmash and wide usmash images match the reviewed correction. CI used Mesa/Xvfb; it establishes neither physical input nor hardware performance.

A separate release build from main produced executable SHA-256 `899c2d0d37dde5fdf7f8ef25ec8087f80d874fe0251f693d6dc3ddd9bea5ebb5`. Its Windows/NVIDIA capture completed with 60 files and no skips. Independent audits again passed 2,648 grounded foot checks, 234 active contacts, 1,662 support measurements and 264 rendered cells. The 2,743 simulation rows and runtime tables match the reviewed candidate.

The identified main executable also opened a visible menu and animated attract scene with an isolated test configuration. It displayed one detected gamepad. Computer Use key injection did not change the menu; the user physically pressed Down and confirmed that the selected option changes. Record this as user-confirmed menu keyboard navigation, not agent-verified gameplay or controller operation. Full keyboard gameplay, gamepad buttons, hotplug, rumble, fullscreen and frame pacing remain unverified. These observations do not close the Kestrel art gate or authorize an Alpha release.
