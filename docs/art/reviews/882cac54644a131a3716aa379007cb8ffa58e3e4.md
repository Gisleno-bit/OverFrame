# Kestrel directed-animation checkpoint — 2026-09-11

**Verdict: integrate the tested technical iteration; visual iteration remains required.** This does not approve Kestrel, Windows/controller QA or Alpha 0.7.

## Provenance

Claude supplied `overframe-fix-support_port-select_diag-motion-both-facings.zip`, SHA-256 `3974d754515b4e31f62c879bca73e1dd9397dac2fe7cbb4de632e86a4b1e4dd2`. Its 15 implementation files were inspected, hash-verified and tested in the isolated Windows candidate `882cac54644a131a3716aa379007cb8ffa58e3e4`, based on `4c46c7b`. Game main was `91b657c4789352dbd1e88e74e11f8c47da201ac3`; its intervening review/direction files are preserved. The independent Electron repository is outside this transfer.

Candidate release executable SHA-256: `afacba849010e78bfab87fc9d027a5b1723ea34ba2a8d8f877ddb3e1f356c651`. Captures used the actual Windows OpenGL renderer (NVIDIA 616.56). This candidate SHA identifies the local evidence; a subsequent main commit and its CI images must retain their own source identity.

## Implemented and checked

The delivery evaluates Kestrel's directed action poses against actual runtime timing, adds contact/support diagnostics, fixes caption layout, and exports both-facing PNG motion sequences with 60 Hz sidecars. Support diagnostics select their requested fighter independently of contact queries. Synthetic probes identify their method; rendered-cell measurements are checked separately. Neutral-special's actual projectile emission and later fighter hitbox remain distinct evidence events.

- Windows formatter and Clippy (`--all-targets --all-features`, warnings denied) passed.
- `cargo test --all-features`: 161 passed, 0 failed, 3 ignored diagnostic tests.
- `cargo test --no-default-features --features headless`: 141 passed, 0 failed, 3 ignored diagnostic tests.
- Python evidence suite: 21 passed. Release build passed.
- Standard capture: 60 files, 0 skipped. All 234 actual active fighter contacts have finite, nonpositive separation. All 1,662 support measurements pass the independent two-foot/reference audit.
- All 264 rendered primary/wide cells' contact/support measurements match the independent replay samples exactly.
- All 2,743 gameplay CSV rows and character/stage/frame-data tables equal `91f9c5a` after excluding source-SHA labels. This delivery changes presentation and evidence, not those simulation fixtures.

Local evidence is retained under `work/game-evidence/windows-882cac5` in the coordinating Windows workspace. The complete motion review inventory contains 46 sequences (23 variants, both facings), 1,658 PNG frames. The initial aggregate motion process left no final index and an incomplete back-facing down-throw export; a fresh, separately indexed `--only throw_d-motion` capture completed both down-throw facings (2 files, 0 skipped). The combined review inventory uses that replacement pair and does not pretend the initial aggregate capture completed.

The GIF encoder quantizes the intended frame delay; those GIFs are previews, not timing evidence. A lossless APNG of SpecialN was independently checked frame-by-frame against all 34 raw PNGs, with each decoded delay exactly 1/60 second. No frames or pixels were changed by that conversion.

## Actual visual review and limits

Sampled tracking contact sheets were inspected for every one of the 23 action variants, plus the critical mirrored dsmash, usmash, SpecialN, dash attack and down throw. These sheets preserve frame/tick labels but crop around the fighter; the original sequences retain their fixed cameras. This is pose/phase inspection, not a claimed full-rate playback or human gameplay test. The current aerial fixtures land before some natural aerial recoveries finish, so they also demonstrate landing interruption rather than every complete air recovery.

The lower-body correction gives the support knee real flexion; the dash attack has a bent leading forearm; foot support is now measured instead of reporting empty lists or sentinel values. These are useful improvements. The following remain open:

1. **Dsmash identity:** it still reads as a horizontal forward kick close to dtilt. A low heel sweep needs a lower intended contact point within the existing region, a seated pelvis, diagonal attacking leg and distinct counterbalance. See [the next physical direction](../procedural/anim/kestrel-physical-iteration.md).
2. **Usmash release:** compression increases into active frames and remains there. Load the legs during startup/charge, then extend into the active upward strike. Utilt should remain the smaller, compact anti-air.
3. **SpecialN:** the new contact solution satisfies the real mesh measurement, but the torso folds excessively behind the arm and the discharge resembles a long punch. Decide its gameplay after executable baseline diagnostics, then direct a compact original zap. Do not remove a hitbox solely to fit the pose or weaken actual contact assertions.
4. **Throw/readability follow-up:** the back-throw fixture visibly sends its victim forward. Establish the launch-direction cause in simulation before adjusting the presentation around it. Other throw poses and aerial end states still need full-cycle playback after the gameplay decisions.
5. **Feedback:** large gold active-region discs obscure parts of the pose. Their restraint and Kestrel-specific energy expression remain part of the scheduled feedback pass; numerical contact does not approve the current effect treatment.

The earlier interactive Windows attempt only established that the game process/window opened. Computer Use timed out awaiting app approval before actual input inspection; keyboard/gamepad functionality was not verified. Version remains 0.6.0; do not tag or publish Alpha 0.7 from this checkpoint.

## Next handoff

Claude is first delivering executable baseline diagnostics for frontal shield/powershield projectile interactions, idle-versus-shield SpecialN input emission, effects of the later zero-damage fighter hitbox, and ThrowB/Bair launch signs with neutral DI in both facings. These establish current behavior, not permanent requirements to preserve bugs. A separate visual-only low-sweep/usmash iteration is directed while the gameplay decision remains pending.

Continue the user's order in [ALPHA_0_7.md](../ALPHA_0_7.md). Kestrel is still the active quality gate.
