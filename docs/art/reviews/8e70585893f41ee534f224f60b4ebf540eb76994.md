# Compact Kestrel Special N — incremental Windows acceptance

Reviewed 2026-09-12. Implementation source `8e70585893f41ee534f224f60b4ebf540eb76994`, based on ec480ab, is accepted as an incremental presentation and evidence improvement. Integration preserves the newer [animation ownership direction](../ALPHA_0_7.md): Kestrel continues; the owner supplies later fighter animations unless explicitly requesting further work.

## Result and scope

The [compact discharge direction](../procedural/anim/kestrel-special-n-presentation.md) now has one bent-elbow release, a brief withdrawal and one return to guard. The old large forward hold is removed; there is no second punch at the old melee marker. Kestrel's projectile is a small cyan-white faceted pulse with a short taper and restrained release flash. Other fighters retain their projectile presentation.

Ground, short air, complete air and shield entry are captured in both facings. The full-air recipe records exactly 32 actual Attack/SpecialN state frames, on consecutive ticks 65–96, all airborne, followed by Air at tick 97. The short-air recipe deliberately lands after state frame 12. Returning visually to guard does not shorten the existing runtime commitment.

The actual emission origin remains local (14,15), radius 4; the first integrated projectile is at (21,15). All eight palm-surface tests meet the origin region: signed gaps approximately -0.1355 ground, -0.1152 air/air_full and -0.1406 shield entry. The centroid remains outside and is not substituted for the actual projected mesh test. The head sits inside the collision radius; the cosmetic taper extends outside it. No simulation, damage, flight, lockout, hitbox, capsule, root scale or bone-length adjustment was made for presentation. The four shield correction files remain unchanged.

## Source provenance

Claude authored the implementation, tests and capture changes. The reviewer inspected the delivery and transferred exact bytes. Rev2 archive SHA256 `66e7c9bd0789e4d5795981de8a9a184c35106967851ea418a99d938763127c9b`, plus the two-file rev2c delta, yields seven source hashes with aggregate `4418d555a8de2c3fbdec81b498607f4bc57932295569ead43d5b4216001657c5`. Both new patches independently reconstruct the delivered files. Five source files are byte-identical to rev2.

The three rev2c evidence archives verify all 553 payload entries. Archive hashes: part1 `5bfaeb0cd0bcc6029967a3604ae6c86c2810db393bbe0f9f5d621fcabdd1fd91`, part2 `491e1178e4540720daa130e5cc1db313f1491750f6b3c88cd9038d91f6fabcc5`, part3 `281b1be2e6f5dc02897ef27cfe7672c73408c55b6e1930d093ffe269d9018efb`. Part1's PARTS.txt retains its pre-insertion archive hash a63cc7d2. Claude confirmed the actual external 5bfaeb0c hash; the preserved payload was not repackaged. These VM frames are labelled uncommitted WIP by the complete source aggregate, not falsely labelled as the Windows commit.

## Independent Windows evidence

- Format and all-target/all-feature Clippy with warnings denied pass.
- Full-feature Rust: 220 passed, zero failed, six reporting diagnostics ignored.
- No-GUI netcode/gltf: 213 passed, zero failed, six ignored.
- Python evidence: 26 passed.
- Offline release build from the clean implementation commit passed. Executable SHA256: `76634fe197be9a6ba76d0fff99e19a7c703347513f13b562b9b9253145fc76c0`.

Production Windows capture uses macroquad/miniquad OpenGL 3.1.0, NVIDIA 616.56. The full suite produced 64 indexed files; the dedicated Special N run produced 24 indexed entries including 16 continuous sequences, 464 frames, with no skips. All 286 drawn cells agree with independent replay; 232 existing active contacts, 1,826 support measurements and 2,788 grounded foot measurements have no audit failures. Existing 2,743 causal CSV rows and character/stage tables match baseline Windows 888, excluding source identity. Three new Special N variants are reported separately. All 27 hit/block/whiff spacing rows already agreed between baseline Windows 888, rev2 Windows and the VM; rev2c changes no causal or pose source.

All 464 Windows frames were personally inspected through sixteen unscaled tracking sheets. Original ground release/impact gamecam images and ground/full-air wide diagnostic sheets were also inspected. A single compact discharge, recoil and recovery are readable in both facings. Support remains stable and the actual dummy impact reads clearly. Tracking sheets are inspection aids, not trajectory proof. Sixteen lossless APNGs were decoded and every frame's pixels and exact 1/60-second delay checked against originals; personal full-rate playback and interactive frame pacing are not claimed.

Local evidence: `work/game-evidence/windows-8e70585` and `work/game-evidence/windows-8e70585-special-motion` in the coordination workspace. The capture source commit is retained in integration history. Documentation-only merge differences do not relabel that binary or evidence.

## Remaining work

Windows gamecam shows normal orange Kestrel and grey Boulder, while same-source Mesa perspective captures show nearly black fighters and some different stage surfaces. A width-only diagnosis was rejected and withdrawn by Claude. A separate matched-camera/material diagnosis is investigating depth, projection and backend behavior. Fixed cameras retain their diagnostic outline density; the gamecam capture now uses the interactive outline path, shake and hitstop wash, with HUD deliberately omitted.

This is partial Special N acceptance, not full Kestrel animation, camera/platform consistency, controller/performance or Alpha release approval. Matching remote CI/evidence will be checked after publication. [Charged-release continuity](../procedural/anim/kestrel-charge-release-presentation.md) and [the four throws](../procedural/anim/kestrel-throw-presentation.md) remain the next Kestrel animation work. Claude's separate charge experiment must not be mixed with the preserved rev2c delivery.
