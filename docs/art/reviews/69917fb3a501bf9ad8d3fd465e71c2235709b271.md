# Kestrel Alpha 0.7 — Windows review, checkpoint 2

Source `69917fb3a501bf9ad8d3fd465e71c2235709b271` is an isolated, unpublished WIP checkpoint, not game `main`. Source archive SHA-256 `5090fa44ffbba88af015e780dcb99e842a4ff25b0c15702f2d577098875ae5a8`. Windows executable SHA-256 `b4df685bbefc414eb3ae0ed3bafe7c0d08f855b13143747dff7a0844bab151ab`. Local capture `work/game-evidence/windows-69917fb`, real game3d/OpenGL/NVIDIA 616.56: 60 indexed images, zero skips. All 23 variants have expected piece identities and both facings. The 2,743 frame-data rows and character/stage/case exports match implementation 91f9c5a exactly, ignoring only the source label.

**Verdict: iteration required. No full animation pass.** All 23 primary sheets and the SpecialN fighter-hitbox supplement were inspected. Bair/nair and all four throws also received wide-sheet inspection. Complete motion, remaining wide sheets, mirror/palettes and final-source review remain pending.

## Confirmed progress

- Bair points the left heel behind the hips; its wide sheets retain the contact through the real late interval. Both facings have 16 active samples, no positive separation, worst approximately -5.652 units.
- Nair retains the forward sole through its real airborne activity, then transitions into the actual landing. Both facings have 23 active samples, no positive separation, worst -10.500 units.
- Ftilt, fair, uair and dair now have visibly directed contact feet. Utilt reaches overhead with the correct hand. The source does not change simulation values.
- The SpecialN supplemental sheet now shows the real later fighter hitbox separately from projectile emission. Its current gap is still positive, approximately +0.105 units.
- A first-line pixel audit of 72 primary caption cells found no wholly missing glyph. Enlarged raw-pixel inspection of fsmash captions is readable. Do not revive a missing-glyph/renderer fault from the reduced image preview.

## Finish the current technical correction

1. Correct SpecialN with a coherent pose and preserve emission/support. Its torso already leans substantially; additional lean must receive visual inspection. The final tests must require every active contact to intersect, including this action, and require present finite measurements. Checkpoint 2 changed tests to expect this failure; those exceptions are rejected as final acceptance criteria.
2. Fix support.png. All four cells export empty `feet` and display `f32::MAX` as lowest foot. `draw_fighter_measured` populates `out.support` only inside a matching ContactQuery branch, but the support fixture requests capsules without a contact query. Support measurement must be independent of an attack query, on the same evaluated pose and explicit port. Require two actual foot IDs with finite bounds/clearance in every support cell; fail generation/validation on absent data instead of serializing an initialized sentinel.
3. Mark synthetic animation/feasibility values explicitly in the exported JSON, not only Rust comments. Make the two SpecialN supplemental images required by evidence validation. Preserve actual grounded versus airborne support-reference labels.

## Next visual correction, after the technical checkpoint is delivered

### Low attacks and upward smash need real leg compression

Observed in `contact-dtilt.png`, `contact-dsmash.png` and `contact-ftilt.png`: dtilt and dsmash have nearly the same upright body/support leg and chamber as the ordinary forward kick. The lower contact height alone does not communicate the authored low poke versus committed low sweep. Dsmash does not visibly sit its hips over a bent support leg. `contact-usmash.png` similarly has almost the same upright legs and upper-body silhouette as utilt, despite its requested compression/release.

Likely cause to measure: `staged` subtracts the family's crouch offset, but the unchanged support-leg angles place the foot below the floor; `support_lift` then raises the whole body, canceling that lowering. Measure achieved hip/head height and both support feet at windup/contact/recovery rather than treating a nonnegative foot minimum as proof of an authored crouch.

Required physical result:

- Dtilt: moderate support-knee flexion, hips lower than ftilt, near hand guarding high and rear hand counterbalancing. A low forward sole, knee retracts before rising.
- Dsmash: distinctly deeper seated hip and folded support knee, broader grounded brace, stronger torso counterbalance and an unmistakable low heel-sweep preparation/recovery. Do not invent rear damage.
- Usmash: a short but visible two-leg compression during available startup/charge, then an upward palm-driven uncoil. Keep utilt as the compact upright uppercut.
- Solve grounded leg articulation coherently against the real plane; do not lower a rigid leg and undo it by lifting the entire fighter. Preserve bone lengths, scale, simulation root and collision geometry. A tiny corrective support lift is a residual clearance correction, not a replacement for a bent knee.

The contact piece only needs to intersect the existing runtime region. It need not chase its centre if that creates the wrong physical action. An authored target inside the real region may be selected for a low sole, while actual mesh-to-hitbox measurement remains the acceptance boundary. Report genuinely unreachable combinations.

### Dash attack must retain a bent forearm

Observed `contact-dash_attack.png`: its strike is a fully extended punch, very similar to `special_side` and fsmash, despite the authored running-forearm direction. The universal centre-seeking IK fully extends a short arm even when the broad hit region permits a closer contact point.

Keep the leading elbow visibly bent and forearm braced in front of the chest, with off arm trailing and running legs separated. Choose an authored point within the real hit region, preserving exact hand-piece intersection, rather than maximizing reach toward its centre. Side-special retains the straighter lateral palm drive, and fsmash retains the more committed body-supported thrust.

### Complete motion evidence is required

The current primary sheet shows last inactive, first active and first recovery; the first recovery frame naturally resembles the just-finished contact. These three samples do not establish anticipation, recovery paths or the absence of a second swing. Add an optional capture selection for action motion using existing real case replays and the production render evaluator. Cover all 22 actions/23 variants, including charge and jab2, in both facings; retain the established primary/wide camera sheets unchanged.

Use a declared supplementary camera with enough room for the whole fighter and the rear/up/down strike, and a legible playback cadence derived from the 60 Hz replay. Include the genuine windup, all active/late activity, hitlag and recovery/landing/release. Preserve real victim and projectile events. Record source SHA, case, ticks, facing, camera and sampling cadence. Reuse the existing GIF/capture utilities; do not build a new animation system. A focused motion batch may be delivered first for low attacks, smashes, dash attack, bair/nair and SpecialN, followed by the remaining cases before approval.

## Gameplay follow-up kept separate from the animation correction

The four throw wide sheets expose an existing problem: back throw releases the victim toward the original facing, like forward throw, while the authored hand points behind. In the facing +1 sheet the victim is still to the right after release. `apply_hit_with` mirrors the positive 45-degree launch only by attacker facing; ThrowB's negative hitbox offset does not change that direction. Confirm with a neutral-DI runtime assertion on initial launch and compare both facings/characters. Back throw must send behind by design. Back air uses a rear hitbox with a forward-facing launch convention too; test its intended outward launch separately.

Do not hide this by moving the hand to match an incorrect throw. Address it as an explicit gameplay correction with before/after evidence, separate from the currently unchanged-simulation animation checkpoint, along with the SpecialN shield/emission concerns in the gameplay review direction.
