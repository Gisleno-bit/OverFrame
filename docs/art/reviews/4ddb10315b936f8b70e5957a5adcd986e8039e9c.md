# Kestrel gameplay correction — Windows review, 2026-09-11

Reviewed implementation: `4ddb10315b936f8b70e5957a5adcd986e8039e9c`, based on `15b83866761adbb4f53f46e50d54bd62737124d9`. This accepts a scoped gameplay correction for integration. It does not approve Kestrel's complete visual gate, Windows hardware QA or Alpha 0.7.

## Source and behavioral verification

Claude's revision-2 source archive SHA-256 is `e38048eccb4d99a5fe70354d46bf1d3d208ff92023390bb0da11775303eddbe7`. All 21 manifest entries were verified; ten exact implementation/test/tool files were transferred to an isolated checkout. The source fixes frontal projectile shield/powershield response, actual projectile emission from the existing shield entry, preservation of unrelated owner hitlag, deliberate removal of only Kestrel's zero-damage SpecialN melee placeholder, and rearward Bair/ThrowB launch across all three Alpha fighters. Timing, numeric attack data and model dimensions remain unchanged. Low sweep and upward smash corrections are preserved.

The initial d764 delivery was not accepted as complete regression evidence: its test referenced the new `no_melee` field and could not compile on the baseline, its late-hit classification relied only on damage, and the manifest validator coerced malformed values to false. Revision 2 fixes these defects and the inaccurate export description/provenance hash. Historical archives/logs remain preserved.

The same portable test file, SHA-256 `a6fc37bf5a13858926ef238ace0a3f8347db675f256213c46302e6cf70cf3966`, ran on baseline `019c2b141b0ddce864c35fcd03ec7163922e85cd` and the corrected candidate. Baseline tracked files remained unchanged. Independent Windows caches produce **8 pass / 12 behavioral failures before**, **20 pass / 0 failures after**, with no compilation error. An earlier shared-cache run falsely passed the baseline by reusing the corrected library; it is explicitly discarded.

All 18 rearward cases are logged: three fighters, two facings, clean/late Bair and ThrowB. Comparing both runs shows only the intended horizontal sign reversal at logged precision, with identical damage, vertical speed and magnitude. Twenty-four other matrix rows (Fair/Uair/Dair and ThrowU/ThrowD, with ThrowF additionally recorded alongside ThrowB) remain identical. Aerial connections assert the actual move, airborne state, facing and clean/late frame window. The regression also checks rearward DI.

The frontal-shield regression samples the impact tick: actual shield age/state, exact normal shieldstun and push, shield-health cost, no body damage/hitlag/pending launch, and consumed projectile. Fresh powershield takes no extra push/stun/damage. Back-facing and unshielded controls still take damage. This covers a single incoming projectile; consecutive impacts during normal shieldstun remain a separate source-derived question, not an established fix or failure.

Windows checks: format and warnings-denied Clippy all-targets/all-features passed; **197 Rust tests passed**, four report-only diagnostics ignored; headless `netcode gltf` **190 passed**, four ignored; Python evidence **26 passed**. Determinism, rollback/checksum and automated loopback host/join tests passed. These are automated tests, not two manually played online instances or physical controller verification.

## Visual scope and remaining direction

The gameplay WIP and revision 2 share identical animation/capture source. All 68 SpecialN motion frames across both facings show one immediate release and one return; the second punch is removed. Event selection changes the actual animation even though no new pose family was authored. The large lean/hold, yellow orb and misleading old motion phase labels remain for the [compact discharge pass](../procedural/anim/kestrel-special-n-presentation.md).

All 82 back-throw frames across both facings now show the victim travelling behind the attacker. The body pose still needs a clearer backward twist/glance. Preserve the real held-target and release trajectory when refining presentation; do not teleport the target to satisfy an art pose. More observations are in [the continued motion review](kestrel-motion-20260911.md).

Claude's revision-2 evidence archive SHA-256 is `866918e89f8f3eb84a92d1727a1007b0ca5a650e0d0f9758b9dc527104d4f6e4`; all 81 manifest entries were verified. Its WIP source labels are preserved and are not presented as clean-commit evidence. Local verification artifacts are in the review workspace under `work/gameplay-fixes-rev2-20260911`.

## Clean-commit Windows capture

The release executable built from this clean candidate has SHA-256 `c25988cf63a9885e9b65876b81f6a7a3a9e1153151a012cff35d0a722e4604a2`. The NVIDIA/OpenGL production capture, labelled with the full candidate SHA, completed with 58 files and no skipped images. Its SpecialN wide view and ThrowB primary view were personally inspected and match the scoped correction; the old phase-label issue remains explicitly pending.

Independent audits passed all 232 actual active contacts, 1,662 support measurements and 253 rendered cells against replay. All 2,648 grounded foot measurements have valid data and no penetration exceeding 0.01 units. The negative root-relative SpecialUp measurement is airborne and is not ground penetration. SpecialN contains actual projectile evidence and no fighter-hitbox tick in either facing, so the removed supplemental views are correctly absent. All 2,743 simulation rows and the character/stage/frame-data tables match the previous corrected WIP, excluding source labels. These CSV rows do not themselves measure victim launch velocity; that proof comes from the independent before/after regression traces above.

Local captures: `work/game-evidence/windows-4ddb103`. Full Kestrel art approval remains pending. The next gameplay probe concerns consecutive shield impacts; the compact discharge and more physical back-throw pose are the next visual refinements. No version bump or release tag is part of this integration.
